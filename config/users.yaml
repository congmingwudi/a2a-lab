# Lab users (WS6 U1) — the demo-scale identity provider's directory.
# No passwords: the console login is a user picker (demo ethos — the point
# is propagation and authorization, not credential UX). Roles:
#   operator — runs experiments, sees all traces/obs data
#   viewer   — runs experiments, sees only their own runs (enforced in U3)
users:
  ryan:
    name: Ryan Cox
    role: operator
  ana:
    name: Ana the Architect
    role: operator
  vic:
    name: Vic the Visitor
    role: viewer
