<?xml version="1.0" encoding="UTF-8"?>
<!-- Custom-auth external credential holding the bridge token. After deploy:
     create a Named Principal on this credential in Setup (or via the
     salesforce-dx MCP tools) and set parameter BridgeToken to the value of
     BRIDGE_TOKEN from .env, then grant the A2ALab integration user access
     via permission set. See plan/04-runbooks.md §Salesforce. -->
<ExternalCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <authenticationProtocol>Custom</authenticationProtocol>
    <externalCredentialParameters>
        <parameterName>A2ALabPrincipal</parameterName>
        <parameterType>NamedPrincipal</parameterType>
        <sequenceNumber>1</sequenceNumber>
    </externalCredentialParameters>
    <label>A2ALab Bridge</label>
</ExternalCredential>
