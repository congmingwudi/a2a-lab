**Verdict: REQUEST CHANGES**

# WS25 batch 3 plan critique — Codex

The plan identifies the right source defects and keeps the batch aligned with
D80, but four decisions remain unresolved in ways that would let the proposed
implementation pass its listed unit tests without satisfying A4, A5, or A11.
Revise the plan before settlement; no implementation should start from the
current version.

## 1. A4's next-tick retry does not retry the failed Salesforce write

`BriefRunner._drive` treats every historical `user.custom_tool_result` as an
answered tool id and skips the matching `agent.custom_tool_use`
(`src/briefs/runner.py#L225-L242`). The current failure path sends exactly such
a tool result after catching the writer exception
(`src/briefs/runner.py#L343-L370`). Reattaching on the next watcher tick will
therefore skip the failed tool call; merely returning `delivered=0` plus an
error can increment the watcher counter, but it does not call Salesforce
again.

Specify a durable replay path. On a later attempt the runner must recover the
failed tool input and invoke `save_brief` again (or persist an equivalent
retryable delivery command) while continuing to skip tool calls that already
succeeded. The result must be per tool call, not only one `delivered` integer
and one error: the system prompt permits one call per account
(`src/briefs/runner.py#L42-L67`), so one scheduled session can partially
succeed. Mark a session `delivered` only when every observed save call has
succeeded; a zero-save/no-error terminated session is not delivery success.

Add a test spanning two watcher ticks that asserts the writer itself is called
twice after the first failure, plus a two-account partial-success test proving
the successful call is not duplicated while the failed call is repaired.
A structured return is preferable to raising because the tool result still
needs to reach the managed session, but it must carry per-call success/failure
data and drive real replay.

## 2. A4 leaves the idempotency boundary undecided and misses the race

D80 explicitly requires a Salesforce uniqueness boundary, and WS25 item 6
records it as part of acceptance (`plan/00-decisions.md#L3356-L3362`,
`plan/07-workstreams.md#L3528-L3532`). The plan's alternative of relying only
on a query-before-insert contradicts that settled scope. Keep
`Research_Session_Id__c` unique and external-id after the duplicate scan; do
not make the metadata change optional.

The proposed sequential “second call inserts nothing” test is insufficient.
Two writers can both observe no brief before either insert. Define the actual
race-safe write path: Salesforce external-id upsert, or insert followed by
handling the uniqueness conflict and re-querying the winning record. Test the
losing-race response and prove it repairs/returns the winning ids without a
second brief.

Task identity also must be settled before implementation. The current Task is
related to the Account by `WhatId` and has no session key
(`src/briefs/salesforce.py#L150-L176`); the brief object has activities disabled
(`salesforce/force-app/main/default/objects/A2ALab_Account_Brief__c/A2ALab_Account_Brief__c.object-meta.xml#L20-L27`),
so “key it off the brief Id via a lookup” is not the current data model. Choose
one queryable, deterministic Task marker/field and list its metadata and
permission-set changes. If full pair idempotency under concurrent writers is
claimed, that Task key also needs an org-enforced uniqueness strategy; an echo
in Subject alone is only a sequential pre-check. Add both partial-pair
directions and a Task race test.

Also define legacy-state eviction. Existing serviced ids have no `at` value
(`src/briefs/__main__.py#L59-L89`), so “evict oldest at” is undefined after
migration. Preserve them deterministically or assign an explicit migration
ordering, and test the 500-entry boundary across mixed legacy/new records.

## 3. A5's trusted Account Id cannot be a loose singular value

One deployment can request multiple accounts and instructs one save call per
account (`scripts/setup_brief_agent.py#L59-L63`,
`src/briefs/runner.py#L64-L67`). The scheduled watcher receives only a session
id (`src/briefs/runner.py#L185-L199`), so it cannot safely thread one
preauthorized `account_id` into every tool call. A model-name mismatch cannot
be merely logged: that can attach one account's brief to another account.

Settle the config as an operator-authored name-to-Account-Id map (prefer a JSON
env value over an inline delimiter, because the current account value itself
contains commas). Resolve each tool call against that map; validate the Id
shape and the retrieved Account, and reject a conflicting binding. When no map
entry exists, use the deterministic name resolver required by D80.

The SOQL tests need adversarial names, not only empty input. Exact-name lookup
must fetch enough rows to detect duplicate exact names, and the optional LIKE
fallback must escape quote, backslash, `%`, and `_` so a non-empty wildcard
input cannot recreate the `LIKE '%%'` problem (`src/briefs/salesforce.py#L107-L126`).
Add those cases and assert no write on ambiguity or binding mismatch.

Finally, the hosted watcher has no `.a2alab/brief.json`; its environment is an
explicit allow-list (`src/briefs/runner.py#L105-L139`,
`deploy/briefs/deploy_briefs.sh#L117-L140`). The plan must include the new
account-map and `A2ALAB_BRIEF_MAX_ATTEMPTS` variables in `.env.example` and in
that deploy script, or the feature will work locally and disappear in Fargate.

## 4. A11's proposed sessionless branch is bypassed by the real A2A server

`AdapterExecutor` always copies `context.context_id` into
`AgentRequest.session_id` (`src/interop/servers/a2a.py#L75-L101`). With the
pinned a2a-sdk 1.1 implementation, `RequestContext` generates a context id when
the inbound message omits one. An executable source check on this checkout
confirmed both `context.context_id` and `message.context_id` are populated for
a sessionless `SendMessageRequest`. Consequently, retaining
`if req.session_id is None` in `AgentforceProxyAdapter`
(`src/platforms/agentforce/proxy.py#L61-L66`) means the proposed
`(platform, subject)` key is never installed on an ordinary hosted A2A call.

For the shim's `session_reuse=True` mode, define whether the proxy deliberately
overrides the SDK context id with the D80 reuse key, or thread a separate
“caller supplied context” signal through the server. Test through
`TokenAuthMiddleware` + the real A2A executor, not only by calling the proxy
with `session_id=None`. The test must prove two verified subjects isolate, two
calls for one subject reuse, and the legacy shared token follows the explicitly
chosen fallback.

A context variable is acceptable for trusted claims if it is set only from
verified JWT claims and reset with its token in `finally` for every request.
Add overlapping ASGI-request tests proving no subject leaks into a concurrent
JWT or shared-token request, including the SDK's background execution path.
Caller-supplied metadata must be overwritten, never preserved, when the trusted
field is stamped (`src/interop/servers/auth.py#L105-L131`,
`src/interop/servers/a2a.py#L75-L101`).

The deployment map is also overstated: `create_a2a_app` is used by the faces
and shim, not by the bridge or console (`src/interop/adapter.py#L38-L55`,
`deploy/shim/handler.py#L41-L55`). The console still needs its own A4 rebuild;
the bridge has no b3 runtime change and should be removed from the deploy list.

## Independent evidence

- `python3 -B -S build-notes/codex/codebase-review/eval-harness/run.py --case
  'regression.*'`: 2 pass, 0 negative controls.
- `python3 -B build-notes/codex/codebase-review/readiness_probes.py`: stops at
  the intentionally historical redaction assertion, as expected after A1; the
  probe is retained and not weakened.
- Directly running the historical F02 and duplicate-worker probes confirms the
  old direct-proxy shared key and overlapping worker shapes. The A11 critique
  above additionally checks the real SDK-generated context that the historical
  direct-proxy probe omits.
- Proxy-free focused baseline:
  `tests/unit/test_briefs_watch_state.py`, `tests/unit/test_af_proxy.py`, and
  `tests/unit/test_agentforce_client.py`: 19 passed. With the ambient SOCKS
  proxy left enabled, the Agentforce-client fixtures fail during httpx client
  construction because `socksio` is not installed; that is an environment
  setup error, not a b3 source failure.

Revise the plan with these decisions and tests, then return it for critique.

## Round 1

Cross-review target: implementation commit `2e98f93`, with handoff commit
`9be2400`, against the settled rev-2 plan and D81.

The implementation fixes the original broad shapes: the outcome ledger is
structured and bounded, both Salesforce records use the settled composite
external-id upsert, account lookup is fail-closed rather than `LIMIT 1`, and the
shim reuse key is derived from the verified subject. The focused tests pass.
The batch is not ready to deploy, however, because the following acceptance
paths remain incorrect or unproved.

### 1. A4 replay sends a second managed result for an already-answered tool call

On scheduled replay, `_drive` builds `answered` from historical
`user.custom_tool_result` events and supplies it as `skip_tool_ids`
(`src/briefs/runner.py:236-253`). A previously failed save is correctly retried,
but `_handle_event` then unconditionally sends another
`user.custom_tool_result` for the same tool-use id
(`src/briefs/runner.py:374-421`). That is not the direct host-side replay D81
settled (`plan/00-decisions.md:3418-3421`): the managed session already has its
answer. If the API rejects the duplicate result, the successful writer outcome
never reaches `_service_once`; the old failed call stays in the ledger and the
write (and notification) repeats on each tick until the cap.

`test_a_prior_failed_call_is_retried` bypasses this condition by passing an
empty `skip_tool_ids`, and its fake accepts duplicate results
(`tests/unit/test_briefs_retry.py:62-73,130-138`). Add the settled two-tick and
two-account partial-success tests through `_drive`/`_service_once`, with the
historical result id in `answered` and a fake that rejects a second response.
On a replayed failed save, call the writer and update the host ledger without
answering the managed tool use again.

### 2. A failed ledger write does not cause next-poll redelivery in the running watcher

`watch` assigns the new, possibly terminal entry to the in-memory `ledger`
before `_save_ledger` (`src/briefs/__main__.py:178-191`). If persistence raises,
the outer handler logs and retains that mutation. The next poll sees the entry
as terminal and skips it, contrary to `_save_ledger`'s stated invariant that a
lost write redelivers (`src/briefs/__main__.py:151-160`). Persist a candidate
copy before replacing the live ledger (or roll back the mutation) and add a
watch-loop test showing that a state-store failure leaves the prior nonterminal
entry eligible on the next tick.

### 3. The claimed idempotency/race evidence is sequential and notifications repeat

The only duplicate-delivery test awaits the two saves sequentially
(`tests/unit/test_briefs_salesforce.py:240-256`); there is no concurrent losing
writer or either partial-pair repair case required by the settled plan
(`build-notes/claude/ws25-b3-plan.md:126-130`). In addition, `save_brief` posts
the in-app notification after every call even when both upserts report
`created=false` (`src/briefs/salesforce.py:270-348`). Concurrent writers, or the
replay failure above after Salesforce commits, can therefore issue duplicate
alerts despite the claimed idempotent delivery.

Add concurrent-writer and both partial-pair tests that model Salesforce's
external-id semantics, and make the alert decision idempotent (for example,
notify only when this call created a member of the pair, with the exact retry
semantics covered). Also fail closed if a 204 upsert re-query cannot recover an
id: `_upsert_by_external_id` currently returns `id=None`
(`src/briefs/salesforce.py:146-163`) and `save_brief` can continue with a
`/None/view` URL and report success.

### 4. A5 omits two explicit resolver requirements

The trusted-map validator accepts any 15/18-character alphanumeric Salesforce
id, rather than requiring the Account `001` key prefix
(`src/briefs/salesforce.py:44,174-177`; plan requirement at
`build-notes/claude/ws25-b3-plan.md:161-165`). The ambiguity errors report only
counts, not the candidate names/ids (`src/briefs/salesforce.py:210-230`), even
though the settled plan and WS25 item 7 require candidates to be listed
(`build-notes/claude/ws25-b3-plan.md:171-187`; `plan/07-workstreams.md:3533`).
Reject a well-formed non-Account id, include the fetched candidates in both
ambiguity paths, and assert both behaviours.

### 5. `/api/briefs/outcomes` is not a console surface and hides a configured-store outage

The implementation adds only the JSON route. Nothing in
`src/console/static/index.html` fetches or renders `/api/briefs/outcomes`; the
only references are the route and route tests. Calling it “Details-shaped” in
the implementer notes does not satisfy the plan's console listing or the
repository's Details-pane definition of done. Add the operator-visible Details
pane, including pending/terminal outcomes, errors, and an honest empty state.

The route also catches a configured PostgreSQL read failure, silently tries a
local file, and otherwise returns `0/0/0` plus “No brief ledger yet”
(`src/console/app.py:1625-1652`). In a hosted console, an Aurora outage is not an
empty ledger. Surface the store-read error distinctly and test it.

### 6. The schema deploy gate queries fields before they exist

The runbook's “scan first” SOQL selects `A2ALab_Delivery_Key__c`, then deploys
that field (`build-notes/claude/reviews/ws25-b3.md:102-115`). On the pre-b3 org
the query cannot run because the field does not exist; once newly deployed, all
legacy values start null, so that particular pre-deploy duplicate scan cannot
establish anything. D81 repeats the impossible ordering
(`plan/00-decisions.md:3397-3399`), so this needs operator settlement rather than
an implementer-only wording change. Define an executable schema migration gate
(including any legacy brief scan/backfill strategy), amend D81/runbook, and
validate the metadata before code that depends on both unique fields is
deployed.

### 7. A11's background/shared-token concurrency gate is still missing

The source implementation's contextvar set/reset and trusted overwrite look
sound, and the JWT-vs-JWT overlap test passes. But the advertised “real chain”
calls `AdapterExecutor.execute` directly inside a synthetic middleware
downstream; it explicitly fakes the SDK envelope/task plumbing
(`tests/unit/test_shim_session_isolation.py:12-15,169-190`). It therefore does
not prove inheritance through the a2a-sdk background task D81 names. The plan's
concurrent JWT-plus-shared-token case is also absent; the shared-token test is
sequential (`tests/unit/test_shim_session_isolation.py:200-203,206-234`). Add a
background-task boundary test where the middleware returns/resets before the
child reads, plus the concurrent JWT/shared-token case.

## Independent evidence — round 1

- `python3 -B -S build-notes/codex/codebase-review/eval-harness/run.py --case
  'regression.*'`: **2 pass**, 0 negative controls.
- `python3 -B build-notes/codex/codebase-review/readiness_probes.py`: exits at
  the intentionally historical A1 redaction assertion. That probe asserts the
  old defect and is retained unchanged; this is expected, not a b3 regression.
- Proxy-free focused b3 suites and relevant console nodes: **53 passed in
  1.79s**. The green focused suite confirms the implemented happy paths but
  does not cover the failures above.
- `uv run ruff check` on the changed Python/test paths: clean.

Verdict: request changes. Do not run the production schema/code deploys until
the replay, persistence, resolver, console, and migration-gate findings are
answered and the missing concurrency/background cases are covered.
