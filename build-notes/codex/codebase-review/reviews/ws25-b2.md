**Verdict: PASS**

# WS25 batch 2 plan critique — Codex

The proposal is correct, narrowly scoped, and ready for settlement. It fixes
test isolation without weakening the fail-closed assertions or changing
production behavior.

## Source verification

- The failure reproduces with the checkout's ambient `.env`: the three selected
  `main()` tests fail because execution reaches their `uvicorn.run` sentinels
  instead of raising `SystemExit`.
- `tests/unit/test_fail_closed_auth.py:17-21` clears `BRIDGE_TOKEN` in the
  shared fixture. For precision, the fixture does not clear `A2ALAB_TOKEN`;
  the faces and console tests each clear it directly at
  `tests/unit/test_fail_closed_auth.py:155-160` and `:173-178`. This wording
  correction does not change the proposed patch.
- Bridge imports and calls `dotenv.load_dotenv` inside `main()` at
  `src/bridge/app.py:266-272`. Patching `dotenv.load_dotenv` is therefore the
  correct target; `bridge.app` has no bound `load_dotenv` name.
- Console has the same call-time import shape at
  `src/console/app.py:4764-4770`, so the same package-level patch target is
  correct.
- Faces binds `load_dotenv` at module import at
  `src/faces/__main__.py:13-23`. Patching `faces.__main__.load_dotenv` is the
  correct target after importing `faces_main`; patching the package symbol at
  that point would not replace the bound reference.
- The tests already replace the other startup side effects at
  `tests/unit/test_fail_closed_auth.py:74-76`, `:158-160`, and `:176-178`.
  Adding the three proposed `load_dotenv` stubs completes that isolation while
  leaving `require_token` real.

## Scope decision recommended to the operator

Decline the optional production-consistency tweak. Moving the faces import
inside `main()` has no runtime or coverage benefit, and the explicit module
patch accurately follows the existing binding. Keep this batch test-only as
proposed.

## Evidence rerun

- Selected current tests: 3 failed, 20 deselected, reproducing the stated
  ambient-`.env` defect before implementation.
- `python3 -B -S build-notes/codex/codebase-review/eval-harness/run.py --case
  'regression.*'`: 2 pass, 0 negative controls.
- `python3 -B build-notes/codex/codebase-review/readiness_probes.py`: fails only
  at the intentionally historical `redaction_probe`, which asserts the old A1
  leak and is expected to remain red after that defect was fixed.

At implementation, add only the three import-aware stubs, rerun the complete
`test_fail_closed_auth.py` with `.env` present, and confirm both halves of the
faces/console tests still execute: `SystemExit` without the opt-out and
`_Served` with `A2ALAB_ALLOW_UNAUTH=1`.
